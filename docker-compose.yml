version: "3"

services:
  mysql:
    image: mysql:8.0-debian
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw
      MYSQL_DATABASE: miyako_dev
    ports:
      - 3306:3306

  adminer:
    image: adminer:latest
    ports:
      - 8080:8080
